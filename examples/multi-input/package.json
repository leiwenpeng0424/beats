{
    "name": "multi-input",
    "version": "0.0.0",
    "main": "bin/cli.js",
    "scripts": {
        "build": "beats --clean --sourcemap"
    },
    "devDependencies": {
        "@nfts/cli": "workspace:*"
    }
}
